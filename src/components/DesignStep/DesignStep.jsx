import { useState } from 'react'
import css from './DesignStep.module.css'

export default function DesignStep({ setActiveStep }) {
	const [first, setfirst] = useState(second)
	return <div>DesignStep</div>
}
